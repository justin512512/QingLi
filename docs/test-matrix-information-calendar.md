# 信息万年历发布验收矩阵

## 自动化结果

| 项目 | 验收标准 | 结果 |
|---|---|---|
| Core | 黄历、日期换算、生日/纪念日、未来事件全部通过 | 33 项通过 |
| Infrastructure | SQLite 迁移、历史包、更新回退全部通过 | 35 项通过 |
| Windows | ViewModel、三栏 XAML、提醒、定位、打包检查全部通过 | 146 项通过 |
| 离线历史包 | 366 天、每条 HTTPS 来源、最多 10 条/天 | 366 天、1830 条 |
| 便携包 | 包含主程序、恢复工具、节假日和历史数据 | 481 个文件，68.05 MB，必需文件完整 |
| 便携包 SHA-256 | 成品可校验、可追溯 | `3E174359E69C7559ACC2E8A82E161321DDB3C74749D84089B317C9CC45C378E0` |

## Windows 11 人工矩阵

| 场景 | 结果/说明 |
|---|---|
| 100% DPI、底部任务栏 | Windows 桌面测试助手启动授权超时，需由用户双击成品后验收 |
| 125% / 150% DPI | 待验证 |
| 主显示器 / 副显示器 | 定位算法自动约束到鼠标所在显示器工作区；待实机验证 |
| 顶部 / 左侧 / 右侧任务栏 | 弹窗定位算法已有自动测试；任务栏时钟安全替换本身仍只支持底部任务栏 |
| Explorer 重启 | 保留既有时钟恢复和重新定位保护；待实机验证 |
| 无网启动 | 内置数据，无运行期强制网络请求 |
| 损坏更新包 | 自动回退，测试覆盖 |
| 异常退出恢复系统时钟 | 独立 `QingLi.Recovery.exe`，既有测试覆盖 |

## 性能目标

- 首次弹窗小于 500ms。
- 同进程再次打开小于 150ms。
- 切换月份小于 150ms。
- 隐藏弹窗后不运行高频 UI 定时器；仅任务栏时钟按秒刷新。
